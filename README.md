# Generate Preference Pairs

### Command 
```
 python runners/generate_preference_pairs.py --llm gpt-3.5-turbo --num-dialogs 300 --num-mcts-sims 15
 --max-turns 12 --seed 42 --persona --persuader-use-persona  --preference-output preference_pairs.jsonl
```
